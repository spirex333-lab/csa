# be-nest-module

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build be-nest-module` to build the library.

## Running unit tests

Run `nx test be-nest-module` to execute the unit tests via [Jest](https://jestjs.io).
