# <%= fileName %>

This library was generated with the `backend-nest-module` workspace generator.

## Usage

Run `nx graph` to confirm the project appears in the workspace, then develop your module logic under `<%= projectRoot %>`.
